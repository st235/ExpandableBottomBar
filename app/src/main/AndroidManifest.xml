<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="s">
        <activity
            android:name=".ShowCaseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.JavaActivity" />
        <activity android:name=".screens.XmlDeclaredActivity" />
        <activity android:name=".screens.ProgrammaticallyCreatedDemoActivity" />
        <activity android:name=".screens.CoordinatorLayoutActivity" />
        <activity android:name=".screens.ScrollableCoordinatorLayoutActivity" />
        <activity android:name=".screens.navigation.NavigationComponentActivity" />
        <activity android:name=".screens.StylesActivity" />
        <activity android:name=".screens.NotificationBadgeActivity" />
    </application>

</manifest>
